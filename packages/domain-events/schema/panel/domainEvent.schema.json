{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id": "https://funding-service.ukri.org/schema/panel/event/domainEvent.schema.json",
    "title": "Domain Event",
    "description": "An Event emitted by the Panel Api service.  This schema exists to define the current valid event schema versions supported (note the prefix of the referenced schemas).",
    "type": "object",
    "oneOf": [{ "$ref": "v1/domainEvent.schema.json" }]
}
