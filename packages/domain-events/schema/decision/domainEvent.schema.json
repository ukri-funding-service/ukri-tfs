{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id": "https://funding-service.ukri.org/schema/decision/event/domainEvent.schema.json",
    "title": "Decision Domain Event",
    "description": "An Event emitted by the Decision Domain.  This schema exists to define the current valid event schema versions supported (note the prefix of the referenced schemas).",
    "type": "object",
    "oneOf": [
        {
            "$ref": "v1/decisionEvent.schema.json"
        },
        {
            "$ref": "v2/decisionEvent.schema.json"
        }
    ]
}
