{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id": "https://funding-service.ukri.org/schema/conflict/event/domainEvent.schema.json",
    "title": "Conflict Service Domain Event Root",
    "description": "An Event emitted by the Conflict Service.  This schema exists to define the current valid event schema versions supported (note the prefix of the referenced schemas).",
    "type": "object",
    "oneOf": [{ "$ref": "v1/domainEvent.schema.json" }]
}
