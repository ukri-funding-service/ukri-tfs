{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id": "https://funding-service.ukri.org/schema/notification/event/v1/application/submitter.schema.json",
    "title": "Submitter",
    "description": "the submitter of the application",
    "type": "object",
    "required": ["firstName", "lastName"],
    "properties": {
        "firstName": {
            "description": "the first name of the submitter",
            "type": "string"
        },
        "lastName": {
            "description": "the last name of the submitter",
            "type": "string"
        }
    }
}
